{
  "name": "static-site-example",
  "private": true,
  "dependencies": {
    "@metalsmith/markdown": "^1.3.0",
    "@metalsmith/permalinks": "^2.3.0",
    "jstransformer-handlebars": "^1.1.0",
    "metalsmith": "^2.3.0",
    "metalsmith-collections": "^0.9.0",
    "metalsmith-layouts": "^2.3.1",
    "metalsmith-static": "^0.0.5",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "serve": "^13.0.2"
  },
  "scripts": {
    "start": "nodemon -e * --watch src --watch layouts --watch static index.js",
    "build": "node index.js",
    "serve": "serve build"
  }
}
